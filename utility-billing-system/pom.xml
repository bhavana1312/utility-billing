<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <groupId>com.utilitybilling</groupId>
    <artifactId>utility-billing-system</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>

    <name>Utility Billing System</name>
    <description>Enterprise Microservices Capstone Project</description>

    <!-- 🔐 Centralized Versions -->
    <properties>
        <java.version>17</java.version>
        <spring.boot.version>3.2.4</spring.boot.version>
        <spring.cloud.version>2023.0.1</spring.cloud.version>
        <lombok.version>1.18.30</lombok.version>
        <jacoco.version>0.8.11</jacoco.version>
        <sonar.organization>bhavana1312</sonar.organization>
        <sonar.coverage.exclusions>
        **/model/**,
        **/entity/**,
        **/dto/**,
        **/config/**,
        **/feign/**,
        **/client/**,
        **/exception/**,
        **/constants/**,
        **/repository/**,
        **/repo/**,
        **/client/**,
        **/security/**,
        **/filter/**,
        **/util/**,
        **/utils/**,
        **/mapper/**,
        **/listener/**,
        **/event/**,
        **/*Application.java
    </sonar.coverage.exclusions>
    </properties>

    <!-- 🌐 Modules (we will add services one by one) -->
    <modules>
        <module>config-server</module>
        <module>eureka-server</module>
        <module>api-gateway</module>
        <module>auth-service</module>
        <module>tariff-service</module>
        <module>consumer-service</module>
        <module>meter-service</module>
        <module>billing-service</module>
        <module>payment-service</module>
        <!-- <module>report-service</module>
        <module>notification-service</module> -->
    </modules>

    <!-- 📦 Dependency Management -->
    <dependencyManagement>
        <dependencies>

            <!-- Spring Boot BOM -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring.boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Spring Cloud BOM -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring.cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

        </dependencies>
    </dependencyManagement>

    <!-- 🛠 Build Plugins -->
    <build>
    <plugins>
        <!-- JaCoCo -->
                <plugin>
                    <groupId>org.jacoco</groupId>
                    <artifactId>jacoco-maven-plugin</artifactId>
                    <version>${jacoco.version}</version>
                    <executions>
                        <execution>
                            <id>prepare-agent</id>
                            <goals>
                                <goal>prepare-agent</goal>
                            </goals>
                        </execution>
                        <execution>
                            <id>report</id>
                            <phase>verify</phase>
                            <goals>
                                <goal>report</goal>
                            </goals>
                             <configuration>

                <!-- ✅ INCLUDE only controller & service -->
                <includes>
                    <include>**/controller/**</include>
                    <include>**/service/**</include>
                </includes>

                <!-- ❌ EXCLUDE everything else explicitly -->
                <excludes>
                    <exclude>**/config/**</exclude>
                    <exclude>**/dto/**</exclude>
                    <exclude>**/model/**</exclude>
                    <exclude>**/entity/**</exclude>
                    <exclude>**/repository/**</exclude>
                    <exclude>**/exception/**</exclude>
                    <exclude>**/security/**</exclude>
                    <exclude>**/util/**</exclude>
                    <exclude>**/*Application*</exclude>
                </excludes>

            </configuration>
                        </execution>
                    </executions>
                </plugin>
    </plugins>

        <pluginManagement>
            <plugins>

                <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>
                <configuration>
                    <source>17</source>
                    <target>17</target>
                </configuration>
            </plugin>

                <!-- Spring Boot -->
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                </plugin>


            </plugins>
        </pluginManagement>
    </build>

</project>
